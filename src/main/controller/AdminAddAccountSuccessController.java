package main.controller;

public class AdminAddAccountSuccessController {
    // All these success controller is empty as we only to show the message
    // nothing more, better to keep each fxml to each controller
}
